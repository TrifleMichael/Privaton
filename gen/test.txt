class FibCalc: {
    f0 = 0;
    f1 = 1;

    def fib(x): {
        print("x: ", x);
        if (x == 0): {
            return f0;
        };
        if (x == 1): {
            return f1;
        };

        return fib(x-1) + fib(x-2);
    };
};

fc = object.FibCalc;
print(fc.fib(2));